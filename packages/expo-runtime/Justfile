import "../shared/just/all-imports.just"

build: _deps
    # Hermes (expo runner) requires import.meta.url to be defined.
    # It's a hack, and I'd like to make it cleaner.
    just cli build-lib {{ justfile_directory() }}/_gen-tao-lib

watch: _deps
    just cli watch-lib {{ justfile_directory() }}/_gen-tao-lib

release-prep: _deps build test
    npx expo prebuild

run: _deps
    bunx expo start --web --ios

JEST_ARGS := "--detectOpenHandles"
JEST_WATCH_ARGS := JEST_ARGS + " --watch --silent --reporter=dot"

[no-quiet]
test *FLAGS_AND_PATTERNS: _deps build
    bunx jest {{ JEST_ARGS }} -t {{ FLAGS_AND_PATTERNS }}


watch-tests *FLAGS_AND_PATTERNS:
    bunx jest {{ JEST_WATCH_ARGS }} -t {{ FLAGS_AND_PATTERNS }}

_deps:
    bun install
