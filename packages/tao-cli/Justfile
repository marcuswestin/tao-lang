set fallback
set dotenv-load
set quiet

run:
    bun run tao-cli.ts help

test:
    bun test

build: _install_deps
    bun build tao-cli.ts --compile --outfile ../../.builds/tao

_install_deps:
    bun install
