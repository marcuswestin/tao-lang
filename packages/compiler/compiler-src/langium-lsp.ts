/** Re-export Langium LSP APIs so consumers (e.g. ide-extension) use the compiler's langium instance. */
export { startLanguageServer } from 'langium/lsp'
export { NodeFileSystem } from 'langium/node'
