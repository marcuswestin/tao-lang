set unstable := true
set fallback := true
# set quiet := true

build:
    bun install
    just _agent-mise-tasks-gen

sort-package-json-files:
    bunx sort-package-json ../../packages/*/package.json
