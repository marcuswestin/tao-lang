import "../shared/just/all-imports.just"

build: _bun_deps

sort-package-json-files: _bun_deps
    mkdir -p ../../.builds/tmp
    TMPDIR="$(pwd)/../../.builds/tmp" ./node_modules/.bin/sort-package-json ../../packages/*/package.json
