/** Re-exports `spawn`, `spawnSync`, and `SpawnSyncReturns` from `node:child_process` so consumers import from `@shared/exec`. */
export { spawn, spawnSync, type SpawnSyncReturns } from 'node:child_process'
