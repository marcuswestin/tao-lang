set shell := ["zsh", "-euo", "pipefail", "-c"]
set fallback := true
set dotenv-load := true
set quiet := true

sort-package-json-files:
    bunx sort-package-json ../../packages/*/package.json

build:
    just _install_deps