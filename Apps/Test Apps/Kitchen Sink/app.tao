use KnifeBlock, RootView from ./counter
use FridgeView
use Col, Row, Text, Box, Button from @tao/ui

app KitchenSink {
    ui AppView
}

view AppView {
    Text value "Hello Kitchen Sink" { }
    StateView { }
    HandlerTestView { }
}

view HandlerTestView {
    TestButton Action TestAction { }
}

// state count = 1

view StateView {
// Button title count, Action IncrementAction { }
}

action IncrementAction {
// count += 1
}

view TestButton Action string {
    Box {
        inject ```ts <RN.Pressable onPress={props.Action}> ```
        Box {
            Text value "Hello" { }
        }
        inject ```ts </RN.Pressable> ```
    }
}

action TestAction message string {
    inject ```ts
        alert("Test Action")
    ```
}

view LoginForm {
// @PasswordInput TextInput { }
// @UsernameInput TextInput { }
// Button "Login" action {
//     loginUser username @UsernameInput.value, password @PasswordInput.value
// }
}
