# Claude Adapter

Follow `AGENTS.md` for the repo guide and `skills/` for task workflows.

Critical command rule: interact with this repo only through `./agent`. Start with `./agent help`, and use the named commands exposed there, such as `./agent rg <pattern>` and `./agent git status`.
`./agent` is the effective whitelist of allowed commands for agents in this repo.

Keep responses concise, read before editing, prefer small surgical changes, test before declaring work done, and do not add closing fluff.
