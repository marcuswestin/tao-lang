# Auto-generated from Justfile - do not edit manually
# Regenerate with: just _agent-mise-tasks-gen

[tasks._agent-cat]
description = "Print a file (read-only helper for AI agents)."
run = "just _agent-cat"

[tasks._agent-fmt]
description = "Format and lint code"
run = "just _agent-fmt"

[tasks._agent-git]
description = "Execute git commands:"
run = "just _agent-git"

[tasks._agent-lint]
description = "Lint code"
run = "just _agent-lint"

[tasks._agent-mise]
description = "Execute mise commands:"
run = "just _agent-mise"

[tasks._agent-mise-tasks-gen]
description = "Generate mise-gen-just-commands.toml from this file"
run = "just _agent-mise-tasks-gen"

[tasks._agent-rg]
description = "Ripgrep a pattern in an optional path (defaults to current dir)."
run = "just _agent-rg"

[tasks._agent-test]
description = "Run tests"
run = "just _agent-test"
