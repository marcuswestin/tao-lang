// TARGET-ONLY FICTION - current selection-block read-query variant.

data MeetupData {
// ... same entities as DataPipeline.tao (omitted duplicate for sketch)
}

app MeetupLite {
  provider InstantDB {
    appId "example-app-id"
  }
}

query MeetupData.Events as HostedEvents {
  Title,
  StartsAt,
  Host = CurrentUser,
}

session {
  CurrentUser Person
}
