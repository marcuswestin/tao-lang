// TARGET-ONLY FICTION — block-query alternative for same domain. See ../../Data and Queries - Design.md.
// Compare: Data and Queries - Design.md#outstanding

data MeetupData {
  // ... same entities as DataPipeline.tao (omitted duplicate for sketch)
}

app MeetupLite {
  provider InstantDB {
    appId "example-app-id"
  }
}

// Illustrative block form — semantics TBD; may desugar to same IR as pipeline
query MeetupData for Events as HostedEvents {
  where Host is Host
  order StartsAt asc
}

session {
  CurrentUser Person
}
