# Gemini Adapter

Follow `AGENTS.md` for the repo guide and `skills/` for task workflows.

Critical safety rule: interact with this repo only through `./just-agents`. Start with `./just-agents help`, and run shell commands as `./just-agents shell <cmd> <args>`.

Prefer local repo patterns, keep edits focused, and use the canonical skill files for task-specific workflows.
