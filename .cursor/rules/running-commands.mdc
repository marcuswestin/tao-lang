---
description: Require agent commands to go through ./just-agents.
alwaysApply: true
---

Use `./just-agents` for all repo interaction.

- Start with `./just-agents help`.
- Run shell commands as `./just-agents shell <cmd> <args>`.
- Do not edit `just-agents.Justfile` unless the user explicitly asks.
- Follow `AGENTS.md` and the canonical workflows in `skills/`.
